import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "de.hdm.stuttgart.yumatch",
  appName: "yumatch",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
